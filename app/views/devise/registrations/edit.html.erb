<div class="box mx-auto mt-6">
  <h1 class="title is-5">アカウント編集</h1>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field">
      <div class="label">
        <%= f.label :email %>
      </div>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"input" %>
    </div>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <div class="field">
      <div class="label">
        <%= f.label :password %> <i>(変更する場合のみ入力)</i>
      </div>
        <%= f.password_field :password, autocomplete: "new-password", class:"input" %>
        <% if @minimum_password_length %>
          <em>※ <%= @minimum_password_length %> 文字以上</em>
        <% end %>
    </div>

    <div class="field">
      <div class="label">
        <%= f.label :password_confirmation %> <i>(変更する場合のみ入力)</i>
      </div>
        <%= f.password_field :password_confirmation, autocomplete: "new-password", class:"input" %>
    </div>

    <div class="field">
      <div class="label">
        <%= f.label :current_password %> <i>(変更する場合現在のパスワードが必要です)</i><br />
      </div>
        <%= f.password_field :current_password, autocomplete: "current-password", class:"input" %>
    </div>

    <div class="actions mb-3 has-text-centered">
      <%= f.submit "更新", class:"button is-link px-6 mt-3"%>
    </div>
  <% end %>

  <div class="has-text-centered">
    <%= link_to "戻る", :back, class:"button px-6"%>
  </div>

</div>
<!--
<h3>Cancel my account</h3>
<p>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>
-->
